[build]
  # استخدام npx لتجاوز مشكلة أذونات التنفيذ في node_modules
  # (هذا يحدث عند نقل node_modules من Windows إلى Linux)
  command = "npx vite build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
