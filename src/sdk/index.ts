/**
 * ════════════════════════════════════════════════════════════════
 *  SDK Layer - نقطة التصدير الموحدة
 *  جميع خدمات النظام في مكان واحد
 * ════════════════════════════════════════════════════════════════
 * 
 *  الهدف: عزل الكود الأمامي تماماً عن قاعدة البيانات
 *  عند تغيير قاعدة البيانات، نغير فقط ملفات SDK
 * 
 *  ✅ الخدمات المصدرة:
 *  - auth      → المصادقة وإدارة المستخدمين
 *  - employees → إدارة بيانات الموظفين
 *  - attendance→ الحضور والانصراف
 *  - settings  → إعدادات النظام
 *  - reports   → التقارير والتحليلات
 * ════════════════════════════════════════════════════════════════
 */

// المصادقة والمستخدمين
export * from './auth';

// الموظفين
export * from './employees';

// الحضور
export * from './attendance';

// الإعدادات
export * from './settings';

// التقارير
export * from './reports';

// قاعدة البيانات (كلينتات)
export { supabase, default as supabaseClient } from './supabase';
export { supabaseAdmin, isAdminAvailable } from './supabaseAdmin';
